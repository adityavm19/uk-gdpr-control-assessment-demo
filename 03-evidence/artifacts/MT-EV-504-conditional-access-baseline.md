# Synthetic Conditional Access Baseline
Approved baseline requires MFA for privileged interactive access. Legacy VPN authentication connector remains outside the principal conditional-access policy while migration is underway. Emergency access accounts require compensating monitoring, but the selected legacy path has no equivalent step-up enforcement.
